# hulactl command reference

Every `hulactl` subcommand grouped by area: Auth & accounts, Users & access, Forms & landers, Site builds, Staging, Operations, Agents. Each command has a one-line summary, full synopsis, all flags with types and defaults, environment variables read, exit codes, and at least one realistic example.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
