# hula-agent binary reference

Operator reference for the Rust sidecar binary: install, flags (`--config`, `--socket`, `--log-level`, `--dump`), lifecycle (signals, exit codes), resource footprint, logging format, and security notes around socket permissions and YAML file mode bits.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
