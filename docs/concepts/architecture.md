# Architecture overview

How Hula's pieces fit together: the unified listener, virtual servers, the storage layer (Bolt + Raft), the analytics path (ClickHouse), and the build pipeline (`hulabuild` in ephemeral containers). Read this first if you're evaluating Hula or operating one in production.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
