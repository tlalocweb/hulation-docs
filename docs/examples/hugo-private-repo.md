# Hugo + private repo

Deploy a Hugo site from a private GitHub repo using either a personal access token or a deploy key. Covers PAT scope requirements, deploy-key generation, secret rotation, and the `creds:` block under `root_git_autodeploy`.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
