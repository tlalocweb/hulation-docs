# Observability

Logs (structured, JSON-line, level-tagged), the ClickHouse-side query patterns for live analytics, and the operational-health endpoints (`/healthz`, `/readyz`). Covers the log schema, useful queries against the visitor / event tables, and how to wire the logs into your shipping stack.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
