# HTTP API reference

Every HTTP endpoint exposed by Hula: visitor-side endpoints (`/v/hello`, `/v/landing`, `/v/form`, `/v/conversion`, `/v/goal`, the iframe variants, `/chat/*`) and admin-side endpoints (auth flow, forms / landers / users CRUD, build endpoints, badactor list). Headers, request bodies, response codes, and which role each endpoint requires.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
