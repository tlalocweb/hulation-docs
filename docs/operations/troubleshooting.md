# Troubleshooting

The most common failure modes, the diagnostic command, and the fix. ACME failures (port 80 unreachable, DNS not propagated, rate limit hit), DB connection retries, badactor lockouts, build container leaks, OPAQUE registration mismatches. Triage flowchart at the top of the page.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
