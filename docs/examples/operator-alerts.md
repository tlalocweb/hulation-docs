# Operator alerts to email + iOS push

Wire up the operator-alert fan-out so build failures, badactor blocks, and goal hits fire to email + APNs simultaneously. Covers the SMTP, APNs, and FCM config, the per-recipient delivery accounting, and how to test the path without provoking a real incident.

!!! note "Stub"

    This page is on the docs roadmap. See `PRD.md` in the repo for the
    planned outline. Contributions welcome via PR against
    [`tlalocweb/hulation-docs`](https://github.com/tlalocweb/hulation-docs).
